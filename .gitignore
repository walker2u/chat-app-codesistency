# General
.DS_Store
.env
.env*.local
.env.development.local
.env.test.local
.env.production.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# IDE & Editor specific
.idea/
.vscode/
*.code-workspace

# Operating System files
Thumbs.db
ehthumbs.db

# Cache files
.eslintcache
.stylelintcache
.prettiercache

# API (Node.js / Express) specific
# Typically in api/
api/node_modules/
api/dist/
api/build/
api/coverage/
api/.env
api/*.log
api/logs/
api/tmp/

# Client (Vite) specific
# Typically in client/
client/node_modules/
client/dist/
client/coverage/
client/.env
client/.env.*
client/public/build/ # If Vite's SSR output is in public/build
client/.DS_Store
client/src/.DS_Store

# Specific to Vite projects
client/.vite/
client/public/hot-update.json

# If you're using TypeScript and compiling to JS in the same directory (not recommended, but possible)
# client/**/*.js
# client/**/*.js.map
# api/**/*.js # If your API source is TS
# api/**/*.js.map # If your API source is TS


# Dependency directories (if for some reason they are not within api/ or client/)
node_modules/